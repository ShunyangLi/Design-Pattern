工厂模式：
优点：
    1、如果想创造一个对象，只需要知道对象的名字就可以通过工厂模式生产出来
    2、提高扩展性，如果想增加一个产品只需要扩展一个工厂类即可
    3、屏蔽产品的具体实现，调用者只关心接口
缺点：
    1、每次增加产品时都需要添加一个具体的类和对象实现工厂，使系统中的类的个数成倍的增长，在一定程度上增加了系统的复杂性
       同时也增加了依赖性

让其子类实现工厂接口，返回的也是一个抽象的产品。

eg:
    1、您需要一辆汽车，可以直接从工厂里面提货，而不用去管这辆汽车是怎么做出来的，以及这个汽车里面的具体实现
